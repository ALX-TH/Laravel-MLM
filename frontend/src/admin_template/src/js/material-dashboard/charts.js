export default class DashboardCharts {

    constructor(config = undefined) {
        if (typeof config === 'undefined') {
            console.error('Не передали настройки в class DashboardCharts');
            return;
        }
        //this.init();
        console.log('CHARTS');
    }

    //init(){
    //    console.log('CHARTS');
    //}

}